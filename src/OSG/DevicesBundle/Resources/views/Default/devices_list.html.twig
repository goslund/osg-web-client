{% block body %}
{# <row>
    <form role="form">
        {% for entity in entities %}
            <div>
              {{ entity.id }}   
            </div>
        {% endfor %}
    </form>
</row>

 #}
    <table class="table table-striped records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Arduino</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
        {% for entity in entities %}
            {# {{ debug(entity) }} #}
            

            <tr>
                <td><a href="{{ path('control-panel_arduino_show', { 'id': entity.id }) }}">    {{ entity.id }}
                </a>
                </td>
                <td>{{ entity.name }}</td>
        		<td>
            		<ul class="listnav" >
	                    <li>
	                        <a href="{{ path('control-panel_arduino_show', { 'id': entity.id }) }}">

	                        	{% if entity.getArduino is not null %}
	                        		{{ entity.getArduino.getName }}
	                        	{% endif %}
	                        </a>
	                    </li>
	                    <li>
	                    </li>
            		</ul>
            	</td>
            	<td>
            		<ul class="listnav" >
                    <li>
                        <a href="{{ path('control-panel_arduino_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>

                    </li>
                </ul>
            	</td>
        	</tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('control-panel_arduino_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}